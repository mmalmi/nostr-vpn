# TODO

- Replace the default self-reported `node_name` of `nostr-vpn-node` with a deterministic petname derived from the pubkey, keep it user-editable, and make local peer aliases override it in UI and `.nvpn` naming.
